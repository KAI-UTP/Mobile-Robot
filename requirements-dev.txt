-r requirements.txt
pytest>=8.0
pytest-cov>=4.1
ruff>=0.3

# fastapi.testclient needs it, and the tests that drive the run controls,
# manual driving and the comparison score all go through TestClient. It was
# already present in my working environment as a transitive dependency, so the
# suite passed locally and every CI job errored on 24 tests at collection.
httpx>=0.27
